name: Generate and Publish Types

on:
  push:
    branches:
      - master
    paths:
      - 'schemas/**'
      - 'package.json'
      - '.github/workflows/publish-types.yml'
  workflow_dispatch:

jobs:
  publish:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Setup Node.js for npm registry
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          registry-url: 'https://npm.pkg.github.com'
          scope: '@christchurchmayfair'

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Generate types
        run: bun run typegen

      - name: Update package.json for publishing
        run: |
          bun -e "
          const pkg = require('./package.json');
          pkg.name = '@christchurchmayfair/sanity-types';
          pkg.version = '1.0.' + process.env.GITHUB_RUN_NUMBER;
          pkg.main = 'sanity.types.ts';
          pkg.types = 'sanity.types.ts';
          pkg.files = ['sanity.types.ts', 'schema.json'];
          pkg.publishConfig = { registry: 'https://npm.pkg.github.com' };
          delete pkg.private;
          require('fs').writeFileSync('package.json', JSON.stringify(pkg, null, 2));
          "

      - name: Publish to GitHub Packages
        run: npm publish
        env:
          NODE_AUTH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
